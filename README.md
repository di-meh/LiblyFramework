# Framework SCSS de Libly

## Installation

Lancer `npm install`

## Compiler le SCSS

Lancer `npm run dev` ou `npm run prod`

## Pages d'intégration

Elles se situent dans le dossier `dist` et sont nommées "page1", "page2" et "page3".
